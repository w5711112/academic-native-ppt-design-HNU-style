---
name: academic-native-ppt-design-HNU-style
description: 湖南大学专属学术/工业级 PowerPoint (.pptx) 演示文稿主线技能。当用户提及“湖南大学PPT模板”、“湖大模板”、“湖大PPT”、“HNU PPT”时启用。严格基于湖南大学参考模板底板、12 套非对称直角多边形排版原型、左下角印章绝对禁飞区与零损坏原生架构输出交付。
version: 5.0.0
release_date: 2026-09-23
status: formal_release
---

# Academic Native PPT Design - HNU Style (湖南大学专属学术 PPT 设计系统)

> **正式发布版本**：**v5.0 Formal Release (2026年9月23日 永久固化正式版)**  
> **体系架构**：Rules 1~41 全规约闭环、QG-01~QG-41 门禁自动化审查、分体式非对称多边形几何、全域零 Markdown 泄漏契约、底层卡片净高防误降级算法与学术零人称白描规范。  
> **质量基线**：自动化门禁通过率 **100% (0 Critical / 0 High)**，正式作为湖南大学学术汇报与组会 PPT 的最高技术执行标准。

本技能专用于构建**湖南大学官方视觉规范、结构严谨、非对称充实、完全原生可编辑**的学术组会与工业级汇报幻灯片，最终交付物必须为纯正无损的 `.pptx` 格式。

---

## 零、绝对真实事实契约与设计红线 (Zero-Hallucination & Core Invariants)

> [!CAUTION]
> **“叫你参考什么，就参考什么，严禁编造任何不存在的内容！”**
> 演示文稿的权威性与严谨性源自**100% 绝对忠实于用户提供的原始工程与学术参考资料**：
> 1. **严禁捏造技术名词与架构组件 (Zero-Hallucination Middleware)**：
>    - 绝不允许凭空捏造未在参考材料中出现的中间件、总线或算法名称（严禁编造 `CycloneDDS`、`MINCO` 等未提及的名词；原文档写的是 `ROS2`、`MAVLink 扩展二进制协议`、`RTSP 视频流`、`WiFi 邻居组播广播`，就必须 100% 严格使用原文档真实技术栈！）；
> 2. **严禁编造具体量化数据与关键指标 (Zero-Hallucination Metrics)**：
>    - 绝不允许臆造任何频率、时延、控制周期或控制精度数值（严禁虚构 `18ms`、`250Hz`、`1.5% 推力偏差` 等伪造参数；必须严格以原文件白纸黑字的指标为准，如 `综合状态广播频率 ≥ 5 Hz`、`局部规划频率 ≥ 5 Hz`、`轨迹优化频率 ≥ 5 Hz` 等）；
> 3. **封面标题字数硬红线 (≤ 17 个汉字，永久入法)**：
>    - **封面大标题字数严格限制在 17 个汉字以内**！因湖南大学专属母版画布尺寸（960 pt）与红色半透明底框（宽 816.45 pt）几何限制，标题字号在 40~42pt 加粗排版下，单行字数超过 17 字必然超出幻灯片最右边缘；
>    - 若原始素材标题过长，必须提炼精简至 17 字以内（如定为 15 字：`多无人机<YOUR_TOPIC>巡检与控制系统`），确保右侧保留 $\ge 40\text{ pt}$ 的充裕安全留白，强制单行垂直居中，绝不折行，绝不溢出右边界；
> 4. **动态自适应配图法则 (Content-Driven Dynamic Visual Allocation)**：
>    - **彻底打破“每页固定两幅图”的机械教条**！配图数量完全由该章节核心内容的机理复杂度与信息密度动态决定：
>      - **纯管理/应急处置决策页 (0 图)**：如应急处置三级矩阵、服务承诺，采用并列卡片自上而下饱满铺排，垂直充满卡片，**不强塞图片，依靠文字逻辑层次与严整字阶呈现压迫感与严谨度**；
>      - **宽幅高精视景/单一流程页 (1 张大图)**：如 4 阶均匀 B 样条凸包绕飞完整轨迹、系统总体概念、V 型开发流程，单张高精大图独占半幅，配以深度机理与公式卡片，主次分明；
>      - **对比与验证页 (2 张对比图)**：如俯仰角阶跃响应对比 + 扰动恢复放大图、激光点云建图 + ikd-Tree 动态平衡数据结构，双图横向/纵向互证；
>      - **多场景集群演化组图 (3 张画廊图)**：如 EGO-Swarm 集群协同，同时展现多机导航完整航迹、窄通道队形压缩恢复、三维交汇对冲避碰 3 张典型高难工况仿真图；
> 5. **对齐 draw-style 淡雅学术三线表规约、全列居中与精准徽章高亮铁律 (Table Precision Rule)**：
>    - 彻底废除突兀沉闷的纯黑/深蓝黑底板（如 `#1E293B`），吸收 `draw-style` 浅色体系精髓，构建与湖大红主题色天然交融的淡雅学术表格：
>      - **表头底板**：采用**淡雅浅陶粉 (`#F8EAEB`)** 或 **淡雅温润暖灰 (`#F4EFEA`)**；
>      - **表头文字**：采用**湖大红深色加粗文字 (`#8B1D23`, 15pt)** 或 **深炭灰 (`#2D3748`, 15pt)**，全列完全居中；
>      - **数据行**：斑马纹交替浅冷灰 (`#FDFBFA` 与纯白 `#FFFFFF`)，数据文字深灰 (`#2D3748`, >=13pt)，全表所有列统一完全水平居中；
>      - **边框**：0.75pt 浅灰细线 (`#E2D9D7`)，绝无粗重竖线；
>    - **表格单元格边距清零与完全居中铁律**：
>      - 表格内所有单元格的 4 向内边距全部清零（`cell.margin_left = cell.margin_right = cell.margin_top = cell.margin_bottom = Pt(0)`）；
>      - **全表所有列（包含第一列）强制统一完全居中对齐**（`p.alignment = PP_ALIGN.CENTER`），彻底根治第一列靠左或左右偏斜缺陷；
>      - **支持自定义列宽数组 (`col_widths`)**：按列精确分配宽度（如 `[140, 340, 400]` 或 `[105, 75, 70, 95, 140]`），杜绝窄列折行与宽列虚空；
>    - **专有短徽章精准高亮铁律 (Zero False Positive Highlighting)**：
>      - **仅对 $\le 8$ 字符的专有状态短徽章**（如 `"100%"`、`"完全符合"`、`"优于"`、`"达标"`、`"自主可控"`、`"通过评审"`、`"M1"`~`"M4"`）允许在第 3 列及之后红色加粗；
>      - **严禁整句加粗标红**：对于包含“通过”等日常动词的自然语言长描述（如“通过第三方软件测试阶段评审”），强制保持深炭灰常规文字，绝对禁止整句误标红加粗！
> 6. **原生 Office Math 公式体根治与颜色防隐形铁律 (Native OMML Equation Rule)**：
>    - **彻底淘汰以普通文本框模拟公式的做法**！在涉及复杂数学公式的页面，必须生成合规 DrawingML 14 的原生数学公式树：`<a14:m><m:oMathPara><m:oMath>...</m:oMath></m:oMathPara></a14:m>`；
>    - **完全等同于 WPS / PowerPoint 中“插入公式”对象**：用户在 PPT 中用光标点击该公式即可直接激活原生“公式工具/设计”菜单，实现 100% 可视化数学编辑；
>    - 支持标准的 `<m:func>`（如 $\min$ 算子）、`<m:sSub>`（下标结构）、希腊字母（$\lambda$）与专业排版间距；
>    - **严禁裸传 LaTeX 源码反斜杠与原始下划线**：严禁在 PPT 文本中直接输出 `\min`, `\lambda`, `\ge` 或裸露 `_`；
>    - **颜色防隐形防死白铁律**：DrawingML 原生公式中，所有 `<m:rPr>` 下的 `<a:rPr>` 必须显式注入深灰字体颜色 `<a:solidFill><a:srgbClr val="242424"/>`！PowerPoint 在渲染 DrawingML 公式时，若未指定颜色，默认继承母版主题的白色字体，在白底卡片上会变成隐形文字！必须强制注入显式颜色；
>    - **角标下沉算法**：正文段落中出现的物理变量下标（如 $J_s, \lambda_c$），必须解析为真实 DrawingML `<a:rPr baseline="-25000">`，彻底消灭下划线 `_`。
> 7. **消灭 0.75 磅细线碎框与宏观大卡片包裹原则 (Macro Card Clustering & No Fragmented Strokes)**：
>    - **严禁在每个文本框或小图片外层普遍套用 0.75pt 细线框（如 `#E2E8F0`）**！碎框会导致视觉破碎、机械廉价；
>    - **纯净卡片法则**：卡片默认设置无边框（`line.fill.background()` / `<a:noFill/>`），依托淡雅背景色（`#FFFFFF` 或 `#F8FAFC`）呈现极简学术质感；
>    - **宏观包裹大框法则**：对于具有相似主题、并列关系或近似含义的一组图文组件（如对比两组方案、图文并列说明），必须使用一个“宏观圆角矩形浅色大框（框线略粗，圆角自然柔和）”去整体包裹，内部元素完全无边框自适应排布，强化模块聚合感。
> 8. **图框长宽比自适应与断句艺术（Slide 18 标杆经验与举一反三）**：
>    - **严禁“大框装小图、比例严重失调”**！插入图片前必须读取真实图片物理长宽比 $AR_{img} = W / H$；
>    - **宽扁图自适应布局**：对于长宽比很大（$AR \ge 2.0$）的横向流程图/链路拓扑图（如宽高比达 2.4:1 的软件链路图），严禁套用 3:2 或正方形狭窄框！必须压缩左侧文字框宽度（从常规 355pt 压缩至约 235pt），腾出 $\ge 350\text{ pt}$ 宽度给宽幅图片展区，彻底消除图上下方大面积死白；
>    - **狭窄文字框的断句艺术**：文字框压缩至 235pt 左右时，标题必须人工精细断句折行（如“空地异构设备\n联合自主导航与搜索”），正文文字采用 14pt 适度拉大行距垂直撑满，消除下方悬空；
>    - **全篇图文动态自适应**：举一反三全面审视全篇，正方图配正方框，扁图配宽展区，竖图配全高区，杜绝一切悬空死白。
> 9. **事实锚定与 renhua 自然工程润色的严密分工法则 (Strict Division of Factual Grounding & renhua Restraint)**：
>    - **双阶段严密分工体系 (Two-Stage Division of Labor)**：
>      - **第一阶段：事实锚定层（绝对决定“讲什么”，零幻觉红线）**：
>        - 演示文稿中出现的所有专业名词、算法分类、硬件拓扑、协议标准、测试数值与应用场景，**100% 必须白纸黑字来源于参考素材原句，或者属于严谨客观的工程同义概括**；
>        - **严禁无据脑补与超范围外延**：素材未提及的特定行业场景（如擅自脑补“变电架构与导线绝缘子”）、素材未包含的假学术概念（如擅自脑补“全工况无差拍”），一律视为一级严重事故绝对禁止！
>      - **第二阶段：renhua 自然工程层（仅负责“叙事节奏与句式结构”，绝不侵蚀语义）**：
>        - **破八股**：彻底打破每个 bullet 字数完全相同（如全都是 28 字）的 AI 机械排比感，长短句自然错落交替；
>        - **工科白描**：采用资深系统工程师客观、平实、严密、求真的工程汇报口吻（遵循“主谓宾机理 + 真实约束/参数 + 闭环状态”）；
>        - **语义边界红线**：renhua 阶段**严禁增添任何未经素材授权的修饰词、形容词或技术名词**，严禁擅自拔高技术结论或无中生有！
> 10. **形状防重叠与上下呼吸安全留白铁律 (Anti-Overlap & Safe Breathing Gap Rule)**：
>     - **上下构件严禁物理重叠**：上下层叠的卡片、表格与图片之间必须保留 $\ge 15\text{ pt}$（推荐 $20 \sim 35\text{ pt}$）的物理安全间隔；
>     - **动态行距与字阶调节防溢出**：善于根据内容量调节行间距（$1.15\times \sim 1.25\times$）与字号（$13.0 \sim 14.5\text{ pt}$），严格计算并控制卡片高度与行数，严禁上方文字溢出卡片下沿压入甚至遮挡下方卡片的标题与正文！
> 11. **左下角印章禁飞区刚性物理拦截 (Hard Geometric Enforcer - 严禁遮挡印章)**：
>     - 湖南大学专属母版左下角 `X < 280.1 pt 且 Y > 350.0 pt` 为**神圣印章禁区**；
>     - **左侧任何形状底部绝对不能超过 Y = 345.0 pt（最大可用高度仅 250 pt）**！严禁以“垂直充满”为由将左侧卡片向下强行拉伸到 350 pt 以下遮盖校徽印章！印章上方卡片平齐截断，下方右侧横贯落地承接；
> 12. **全生命周期零中间文件即时清理机制 (Zero Intermediate Files & Cache Hygiene)**：
>     - **绝不残留中间文件**：生成脚本、转换工具或测试验证过程中产生的任何 `__pycache__`、临时测试脚本、临时导出图像或调试 XML，必须在任务结束时被即时、彻底删除；
>     - 整个技能的 `examples/` 目录下仅保留唯一的最新权威 `.pptx` 范例（如 `投标文件--技术部分--湖大模板汇报.pptx`），绝不堆积冗余版本。
> 13. **同一页内正文字号协同收敛律 (Page-Level Font Size Harmonization，极差 ≤ 2.0 pt)**：
>     - **严禁同一页内各卡片正文字号断崖割裂**！严禁出现一个卡片 19pt、另一个卡片 14pt 这种高达 5pt 的巨大悬殊；
>     - **同一页内所有卡片正文字号的极差必须严格 ≤ 2.0 pt**（黄金区间收敛在 `14.5 ~ 16.0 pt`，极差推荐控制在 $\le 1.0\text{ pt}$ 甚至完全一致）；
>     - 杜绝单个卡片仅因文字较少便贪婪自适应膨胀至 19pt，保证整页视线平稳、字阶梯队严整匀称。
> 14. **图大文精、图片为核心视觉重心法则 (Visual-Dominant Principle - 图大而不删减文字机理)**：
>     - **图片是 PPT 的第一视觉焦点与重心**：无论是代码绘制的高清曲线、三维点云地图、系统架构拓扑还是实机外场照片，图片容器必须充分舒展放大，上层展区宽度给足、高度扩展至 $247 \sim 400\text{ pt}$，彻底打破矮框与拘束感；
> 15. **卡片垂直空间高充实度法则 (Card Vertical Fill Rate $\ge 80\%$, 黄金区间 85% ~ 95%)**：
>     - **彻底根治卡片下半部死白留白**：严禁文字仅占卡片 50%~60% 空间而导致下半部大面积空旷死白；
>     - **行距默认设为 1.25 倍行距 (或 ~20pt)**：长短句舒展折行，段后距动态均分，杜绝文字缩紧在卡片上半截；
>     - **内容量与合理分点协同充实**：卡片内部必须通过精炼充实的技术细节、合理分点数量（如 4 个结构化要点）将卡片垂直空间充实度推升至 **$\ge 80\%$**，理想黄金占用率在 **$85\% \sim 95\%$**（杜绝 100% 贴边与溢出风险），实现“充实、饱满、呼吸感、无死白”的高阶学术版面质感！
> 16. **三层间距解耦与子框间距大范围动态调节法则 (Multi-Tier Spacing De-layering & Flexible Gap Scaling)**：
>     - **打破“A-A框-B框-B”三层间距陷阱**：在保持外围 L 型多边形与印章避让区不变的前提下，严禁无视内容密度死板套用多重内边距与大框间距（防止实际视觉间距高达 30~40pt 导致图小字挤）；
>     - **支持双模态间距解耦**：
>       - **零内边距贴边扩展模态 (Zero-Padding Mode，图大字多首选)**：文本框内部边距置零（`margin = Pt(0)`），图片容器内部边距置零（`padding = Pt(0)`），图片满幅顶满容器边界，文字行宽最大化；相邻框间距大幅收紧至 **`6.0 ~ 12.0 pt`**（较传统 16~22pt 节省大量水平空间），将空间全部回馈给技术大图与更多分点！
>       - **无缝衔接模态 (Seamless Docking Mode，流程/对比面板首选)**：框与框之间的物理间距缩小至 **`0 ~ 4.0 pt`**，相邻卡片紧密咬合或置于同一宏观大卡内；内部保留 **`8.0 ~ 10.0 pt`** 舒适呼吸边距，形成仪表板式一体化无缝面板；
>     - **双模态字阶动态分流（最小字号永久放宽至 13.0 pt，专为大容量多区域服务）**：
>       - **高密度技术详述与多子区域模式 (13.0 ~ 14.5 pt)**：当内容多、分点多或横向拆分为多个并排子区域时，字号正式以 **`13.0 ~ 14.5 pt`**（允许达到 13.0pt 永久修改）为主导字号，承载密集技术细节；同页字号极差严格 $\le 1.0\text{ pt}$；
>       - **标准/精炼汇报模式 (16.0 ~ 18.0 pt)**：如果内容未被标记为密集、分点适中（2~3 点）时，绝不无端压小字号，保持 **`16.0 ~ 18.0 pt`** 的舒展字阶与极佳远视距可读性！
> 17. **通用 AI 模型适用架构 (Model-Agnostic)**：
>     - 本技能全套标准适用于所有高能力通用 AI 模型，不绑定任何特定厂商私有语法，以严谨规范与确定性算法驱动生成。
> 18. **素材资产独立子目录强绑定与最小冗余隔离法则 (Strict Material-Asset Binding, Subdirectory Isolation & Minimum Redundancy Principle)**：
>     - **专属子目录物理隔离 (Dedicated Subdirectory Isolation)**：
>       - 严禁将任何图像、矢量图或工程图表平铺散落在 `assets/` 根目录下；
>       - 每一个演示文稿项目，必须根据其**素材标题或专题名称**，在 `assets/` 下建立独立的专用子目录（如 `assets/投标文件--技术部分/`）；
>     - **素材标题与元数据显式绑定 (Mandatory Title & Metadata Manifest)**：
>       - 每个项目专属资产子目录下必须包含一份 `README.md`，明确标明：① 项目完整标题（如《面向复杂场景的<YOUR_TOPIC>立体巡检系统》）、② 依托素材源文件（如《投标文件--技术部分.docx》）、③ 适用的 AI 模型归属（如 Gemini、Codex、Claude 或通用多模态模型）、④ 资产映射清册；
>     - **最小必要原则 (Principle of Least Asset Redundancy)**：
>       - 每次根据新素材或指派新 AI 模型创建 PPT 时，必须按需精准生成/提取资产，重置或新建专属资产子目录；
>       - 专属目录内的资产总数必须严格等于该 PPT 实际引用的图表总数（$N_{\text{assets}} = N_{\text{deck\_needed}}$），**严禁留存任何孤立未引用的冗余文件或历史死图**；
> 19. **反夸大描述与反伪学术黑话堆砌法则 (Anti-Hype, Anti-Jargon & Zero Unsubstantiated Superlatives)**：
>     - **广告法式绝对化修饰词全面拉黑**：
>       - 严禁出现“彻底消除”、“绝无”、“绝对优势”、“完全杜绝”、“零死角”、“完美解决”、“颠覆性”、“降维打击”、“极限收敛”、“100% 达标·零失真”等轻浮夸大的修饰词；
>       - 必须替换为客观中性的工程学术语：“填补感知盲区（素材原句）”、“减小姿态超调”、“提高跟踪精度”、“满足系统稳定性指标要求”；
>     - **严禁伪学术黑话杂糅**：
>       - 严禁为了“显得高级”而机械拼凑不相干的控制论/电力电子假黑话（如擅自捏造“全工况无差拍”、“收敛于系统底噪极限”）；
>     - **“合理工程概括”的三项刚性检验准则**：
>       1. **内涵同义，外延绝不扩大**：对原文语句进行紧凑压缩时，不得引入素材未提及的特定外延实体（如原文是“屋顶与低矮管道巡检”，允许概括为“空地多视角立体互补覆盖”，但绝对禁止脑补为“变电架构与导线绝缘子全景拍摄”）；
>       2. **量化数字白纸黑字有据可查**：凡出现具体毫秒数、赫兹数、百分比，必须在素材原文中有明确出处；无据数字绝不捏造，未注明的仅作真实定性表述；
>       3. **留存物理与工程边界余量**：真实工程系统皆有物理极限与边界条件，严禁做出不切实际的绝对化全工况承诺。
> 20. **高密内容多子区域横向拆分与充实度守恒法则 (Multi-Zone Horizontal Partitioning & Density Conservation)**：
>     - **长条大框横向切分架构**：当一页内字很多时，文字必须优先划分为 **2、3、4 个矩形子区域横向排列**（例如将底部单一长条框横向切分为 2 个并排子卡片），彻底消除少分点大段文字的沉闷笨重感；
>     - **分点多、单点精**：每个子区域安排 3~4 个技术要点，每个要点精炼短小（25~32 汉字，占用 1~1.5 行），分别针对不同技术维度，既美观大气，又能传达远超大段文字的高价值工程信息；
>     - **充实度守恒铁律**：绝不能说子区域多了就出现大面积死白留白！多子区域切分后，卡片垂直空间利用率必须严格保持在 **$\ge 80\%$ 且 $\le 108\%$**（黄金区间 85% ~ 95%）；
>     - **内容量自适应双模态**：
>       - *精炼/低密页面*：保持大框或单体卡片，主字号 15.0~16.0 pt，舒朗通透；
>       - *高密/多维页面*：果断采用多子区域横向拆分，字号 13.0~14.5 pt，多维饱满，充实无死白。
> 21. **全域字体统一为微软雅黑与纯正 Cambria Math 公式体隔离铁律 (Strict Typography Unification)**：
>     - **全域字体 100% 统一为微软雅黑**：封面、尾页、目录、所有正文、卡片标题、列表、表格单元格、图注、标签中的所有汉字、西文字母（如 ROS2、Qt/QML、MIL/SIL）、阿拉伯数字（如 2026年9月、4月23日、A01）、版本号全域统一为 **微软雅黑（Microsoft YaHei）**！
>     - **彻底取消 Cambria Math 在普通文字和数字中的混用**：普通英文单词和常规数字严禁使用 Cambria Math，必须保持统一的微软雅黑视觉纯度；
>     - **Cambria Math 严格且仅限用于数学公式体**：仅当涉及真实数学物理变量（如 $J_s, \lambda_s, \tau_c, \Delta x$）、数学运算符（如 $\min, \max, \partial, \nabla$）或原生 OMML 公式时，才允许使用 Cambria Math；
>     - **特例隔离**：封面特定开源链接（如 `https://github.com/w5711112/Unified-Scholarflow-Skills`）作为项目特定展示予以保留，不写入通用 Skill 规则。
> 22. **Slide 25 原生 OpenXML 项目符号与 5pt 段距硬核规范 (Native Bullet & Paragraph Spacing Standard)**：
>     - **原生带点项目符号**：列表文字必须注入母版 Slide 25 原生 OpenXML 项目符号 `<a:buChar char="•"/>` 与 `<a:buFont typeface="Microsoft YaHei UI"/>`，并配置 `marL="228600" indent="-177800"` 悬挂缩进；
>     - **严禁手工拼凑符号**：绝对严禁在正文字符串开头手动添加 `"• "`、`"- "` 或 `"* "`，必须由底层引擎原生渲染，确保换行后的首字垂直严密对齐；
>     - **段前距与段后距标准**：每个列表段落的段前距（`space_before`）与段后距（`space_after`）默认统一设为 **5 pt**（底线 $\ge 4\text{ pt}$），行间距遵循全局标准（$1.15\times \sim 1.25\times$）。
> 23. **Slide 2 动态目录与红蓝章节过渡体系 (Dynamic TOC & Chapter Transition System)**：
>     - **长篇汇报必备目录**：凡 30 页以上或 88 页长篇汇报 PPT，必须在首页后及核心章节转折处配置目录与章节过渡页；
>     - **母版 Slide 2 几何标准**：
>       - 左侧：竖排大标题文本框（`left=175.9, top=80.0, w=135.4, h=385.8`），60pt 微软雅黑加粗纯净排版；
>       - 右侧：章节列表支持 **3 ~ 10 个章节** 动态等间距垂直居中排布（根据章节数自动计算步长，整体居中于画布中心 $y=270$）；
>       - **当前章节精准高亮**：当前正在汇报的章节，其圆角矩形序号框、细竖装饰条与章节标题文字全部高亮为**湖大红 (`#A6232B`)**；
>       - **非当前章节统一标蓝**：其余章节全部采用**湖大深蓝 (`#1D4999`)**，层级分明、庄重典雅。
> 24. **多模板库选型决策树与 5~7 框全文字高密构型 (Template Catalog & 5-7 Box High-Density Layouts)**：
>     - **因地制宜，拒绝单调复用**：严禁全篇通篇僵化重复使用 Slide 6 与 Slide 7 两套模板！每页生成前必须根据内容量与是否有图，从母版库中匹配最契合的模板版型；
>     - **母版 Slides 4~24 全构型目录**：
>       - *Slide 15（上下双通栏 + 右下落地卡片）*：适合宏观系统总述、两阶段对比演进；
>       - *Slide 16（非对称 4 框：上左宽、上右窄、下左窄、下右全景宽）*：适合主次分明、全景互证；
>       - *Slide 17/18（5~6 框多维矩阵网格）*：适合多模块并列分解、多维度指标对照；
>       - *Slide 19/24（7 框高密网格：左侧全局卡 + 2×3 矩阵卡）*：全文字高密无图页面的首选排版，容纳海量分点且视线极其整齐；
>       - *Slide 20/21（4~5 框上下平衡对称网格）*：适合结构化流程或四象限对比；
>       - *Slide 22（左卡 + 中双纵列 + 右双叠卡）*：适合输入-处理-输出闭环架构；
>       - *Slide 23（3 列纵向高耸卡片）*：适合 3 大支柱技术、3 阶段演进或 3 方对比。
>     - **多框容量扩展**：卡片数量正式支持 **2 ~ 7 个框**，字多时果断选用 5~7 框矩阵，消除大段文字拥挤感。
> 25. **图文比例反推法则、大比例粗箭头与全页流程图化 (Content-Driven Layout Balancing & Slide-Scale Flowcharts)**：
>     - **图文比例反推法则**：当流程图、拓扑或表格实际内容较少时（如仅描述几个时间节点），容器展宽必须压缩至 $\le 1/2$ 画布宽（~350pt 甚至更小），腾出的另一半甚至大半区域必须划分为 2~4 个结构化文字卡片，彻底消灭空洞巨型色块与大片死白；
>     - **流程图箭头大比例与多向动线**：
>       - 彻底废除微小针眼细箭头，箭头必须使用粗块状箭头（宽 18~28pt，高 12~16pt）或加粗连接线（线宽 2.0~2.5pt）；
>       - 动线支持**向右（横向流动）、向下（纵向递进）、蛇形折返（先向右 $\to$ 再向下 $\to$ 再向左）**多种灵活拓扑，充分利用画布有限空间；
>     - **全页流程图化设计模式**：对于抽象/治理类无截图 Skill，直接使用整页 PPT 作为“流程图”，通过多个逻辑文字卡片与横纵粗箭头相互串联，形成高度结构化的技术动线。
> 26. **科研功能页【Skill名称：核心作用/定位】强对应三阶演进法则 (Explicit Skill-Titling & 3-Tier Cognitive Progression)**：
>     - **标题强绑定 Skill 名称与具体作用**：凡涉及 Skill 的科研功能页，PPT 标题必须直接包含 Skill 名称及其核心定位，格式固定为：**`【Skill名称：具体功能与作用】`**，让听众一眼洞悉该页与 Skill 体系的强逻辑对应；
>     - **三阶认知演进体系 (3-Tier Progression)**：单个 Skill 的展开建议遵循 1~N 页逻辑递进链：
>       - *阶段 1：Skill 定位与概述*（如 `【renhua：让表达更直接、自然】`）；
>       - *阶段 2：Skill 规则与配置机制*（如 `【renhua：把语言偏好写成具体规则】`）；
>       - *阶段 3：Skill 效果实证与前后对比*（如 `【renhua：同一段文字使用前后对比】`）；
>     - **多 Skill 协同分工明确**：多个 Skill 协作时，必须清晰指出哪一步由哪个 Skill 负责（如由 `drone-literature-scout` 调研方向，由 `zotero-obsidian-paper-import` 导入文献，由 `read-paper-analysis-highlight` 执行精读分析）；
>     - **drone-literature-scout 专属两阶段逻辑**：先通过构建 Skill 调研具有潜力、感兴趣且契合课题组的研究方向 $\to$ 再针对该特定方向深入挖掘代表性学术论文与奠基性文献。
> 27. **视觉节奏铁律与连续文字页上限 (Visual Rhythm Law, ≤ 4-Slide Ceiling)**：
>     - **严禁连续超过 4 页无视觉元素**：严禁连续超过 4 页全是纯文字或表格卡片；每 $\le 4$ 页内必须包含至少 1 页视觉化呈现（包括真实工程/软件截图、系统架构图、算法效果对比图、代码生成的图表、AI概念图、或全页多节点原生流程图）；
>     - **彻底告别“连续 10 页一张图都没有”的沉闷与枯燥**：在文字密集的论述章节，善于通过原生流程图、机制图或技术大图打破排版同质化。
> 28. **用户素材与工程原图绝对零裁切铁律 (Zero-Crop Invariant)**：
>     - **严禁任何形式的图片裁切**：严禁对用户提供的任何真实素材图片执行 PowerPoint 裁切（严禁 `crop_left`, `crop_top`, `crop_right`, `crop_bottom` 或 OpenXML `a:srcRect` 非零属性），严禁切除边缘伪装全图；
>     - **必须采用等比居中缩放 (contain) 模式**：哪怕图像长宽比与容器不完全吻合，也必须采取 `contain` 居中等比缩放留白模式完整呈现，底托与大背景自然融合；
>     - **空间不足时的解决顺序**：① 调整图文比例扩大展区；② 选用合适版型（如 Max-Image 模板）；③ 采用 `contain` 留白；④ 精简文字，把次要解释移入备注。**信息完整性绝对优先于卡片边框完全填满**！
> 29. **分体式卡片架构与行高齐平铁律 (Decoupled Card Architecture & Row Height Invariance)**：
>     - **分体式卡片核心设计 (Decoupled Cards)**：彻底废除单体形状同时承载标题、正文与自适应缩放的做法！卡片必须采用严格的三层分体解耦结构：
>       ① **纯几何底托形状 (`b.rect`)**：固定坐标与尺寸，行高绝对齐平，采用淡雅背景色（`#F4EFEA` 温润浅米、`#EAF1F6` 湖大淡蓝、`#F8EAEB` 浅陶粉、`#EEF3E7` 浅青碧），线框背景透明；
>       ② **顶部独立标题框 (`Section`)**：严格置顶于 $y+8$ 处，高度固定 27~28pt，加粗 18~19pt 湖大红（`#A6232B`）或深蓝，绝不与正文混杂，绝不断词截字；
>       ③ **正文自适应文本框 (`Body`)**：独立置于 $y+35$ 处，宽度 $w-20$，高度 $h-41$。内置字号自适应阶梯算法（$14.0\text{ pt} \to 13.5\text{ pt} \to 13.0\text{ pt}$），按字符物理宽度严格换行并校验高度，确保正文在限定框内饱满排布，绝不溢出！
>     - **行高齐平铁律 (Row Height Invariance)**：同一行并排的所有卡片，必须拥有**完全一致的 Top 坐标与完全一致的 Height 高度**！绝对严禁因不同卡片字数差异而单独缩放单框高度导致参差不齐（严禁在多栏卡片上动态赋值不同 `shape.height`）；
>     - **垂直紧凑布局与杜绝中间留空 (No Middle Dead Zone)**：上下排卡片之间的垂直间距严格保持在 **`11.0 ~ 15.0 pt`**（严格参考母版 Slide 21：上方卡片 $117.5 \sim 344.7\text{ pt}$，下方卡片 $356.1 \sim 493.1\text{ pt}$，间距仅 11.4 pt）。**绝对严禁在上下两排卡片之间留下超过 20pt 的空白大坑**（严禁将卡片缩短后固定在 $y=95$，下方卡片固定在 $y=355$，导致中间出现 150pt 的空白断层）；
>     - **空间饱满度与文字充实法则**：卡片必须充分利用母版预留的垂直展区。当使用 5~7 框布局时，卡片高度与母版对齐，每张卡片安排 3~4 个精炼短分点（各 25~35 字），充实度推升至 85%~95%，杜绝“可用大框只填了上半截寥寥几字”的空洞感受。
> 30. **多样化原生流程图组件规范 (Diverse Native Flowchart Pipeline Standards)**：
>     - **四种主流学术流程图拓扑**：
>       1. **横向单行流水线 (`horizontal_pipeline`)**：3~5 节点横向排列 + 粗块箭头（22×14pt），适合阶段递进与工序流转；
>       2. **环形闭环流程 (`circular_pipeline`)**：4 节点闭环循环 + 顺时针流向，适合迭代优化、生命周期与反馈闭环；
>       3. **蛇形折返流程 (`snake_pipeline`)**：双行折返拓扑（第 1 行向右 $\to$ 右端向下 $\to$ 第 2 行向左），高效利用横纵空间；
>       4. **分叉与树状决策流 (`bifurcated_flow`)**：左侧根节点分叉指向右侧多分支节点，适合技术选型、方向拓展与两阶段调研。
> 31. **巨幅学术对比图模板规范 (Max-Image Benchmark Template)**：
>     - **大图专用版型**：专为 `benchmack的对比.jpg`（1143×1080）等大图定制：左侧精炼文字卡（宽 320~360pt，高 395pt） + 右侧巨幅视觉展区（宽 520~580pt，高 410pt）；
>     - **零裁切完整呈现**：大图 100% 完整展现，文字紧凑精炼，视觉压迫感与学术严谨度拉满。
> 32. **核心动机白描与反造轮子叙事律 (Core Motivation Narrative: Anti-Reinventing-the-Wheel)**：
>     - **直击痛点，拒绝虚饰**：必须白纸黑字明确阐述构建 `draw-style` 和 `academic-native-ppt-design-HNU-style` 的核心催化剂与直接动力，正是为了**“避免重复造轮子”**和**“防止一遍又一遍画丑图”**！
>     - 将个人与团队审美、学术规范、工程经验固化为可沉淀、可复用、可自动化的 Agent Skill，形成长期复利。
> 33. **严禁机械断词截字与语义完整性律 (Semantic Heading Invariance & Anti-Word-Chopping)**：
>     - **严禁字符串暴力截断**：严禁使用 `heading[:16]` 这类粗暴按字符切分的逻辑提取标题；标题与要点提炼必须以自然标点句（句号、冒号、顿号）为单位，保留完整词语与学术概念；
>     - **严禁字词流落正文**：绝不允许发生如将“第二层再给公式、符号解释和必要推导”截断为“第二层再给公式、符号解释和必要推”，而把残缺的“导。两层讲同一个关系...”推入正文的荒谬错误！
> 34. **学术语义色彩调和律与 2~4 色配比法则 (Semantic Color Harmony & 2~4 Colors Law)**：
>     - **单页色彩数量刚性约束**：单页内除白色画布背景与深炭灰正文文字外，显性主题色严格控制在 **2 ~ 3 种（绝对上限 $\le 4$ 种）**，坚决杜绝 5 种以上颜色构成的彩虹杂乱；
>     - **严禁盲目轮盘彩虹轮换 (No Round-Robin Rainbow)**：严禁在同级流程步骤或多栏卡片上按列机械轮换 `[BLUE, GREEN, ROSE, WARM]`！同属于同一流水线或同一并列维度的卡片，**必须保持基色统一**（如统一淡蓝灰 `#EAF1F6` 或纯白 `#FFFFFF` 加细边框），通过箭头或局部徽章表达流向；
>     - **严格语义映射体系**：
>       - **湖大红 (`#A6232B` / `#F8EAEB`)**：页首标线、当前章节高亮、核心锚点、关键流向箭头与最终核心突破结论；
>       - **学术蓝/灰蓝 (`#1D4999` / `#EAF1F6` 或 `#F1F5F9`)**：通用技术底座、基准工序、系统输入、环境与平台架构；
>       - **鼠尾草绿/青碧 (`#0F766E` / `#EEF3E7` 或 `#E6F4F1`)**：规则沉淀、经验固化、测试通过、成功验证与长期资产；
>       - **暖赭/米黄 (`#B45309` / `#FEF3C7` 或 `#FDF6E2`)**：**仅用于**警示、痛点代价、评测陷阱、版本漂移风险等负向/边界语义！中性技术介绍（如 MiMo 2.6 Pro、选择记录）严禁乱赋泥棕色；
>     - **空间施色层次与大框套小框 (Container Grouping)**：善于使用“大框套小框”：一组并列卡片外围使用宏观淡灰/淡蓝底托（如 `#F8FAFC`）整体包裹，内部子卡片纯白高对比，既消除单调，又极具学术秩序感。
> 35. **核心分点多维充实与客观事实知识扩充律 (Substantive Multi-Bullet Expansion & Factual Enrichment)**：
>     - **底线分点数 $\ge 2$ 铁律**：任何内容卡片（高度 $\ge 110\text{ pt}$ 时），**绝对严禁仅有 1 条单薄短句**！每张卡片必须包含至少 **2 ~ 3 条** 独立维度的实质性分点；
>     - **客观技术事实合规扩充许可**：严格区分“凭空捏造参数”与“客观常识深度展开”：针对大模型演进节点（DeepSeek V2/V2.5/V3/R1、GLM-4 系列、MiMo 2.6 Pro 等）、开源架构演进与经典学术流程，**明确允许且必须结合 LLM 知识库与公开事实进行实质性充实**（从架构突破、核心参数/特性、上下文窗口、工具链支持与科研选型边界等 2~3 个维度完整叙述），把要点讲深讲透，严禁死板复制两三个词敷衍大面积留白。
> 36. **有机饱满度、同页行距一致性与真实交替宽方差律 (Organic Fill Rate: 60% ~ 95%, Same-Page Line Spacing Consistency & Natural Cadence)**：
>     - **垂直饱满度判定区间：严格 [60% ~ 95%]**：卡片内部文字估算高度占可用正文高度的比例必须严格在 $60\% \sim 95\%$ 区间（底线 60%，消除下半部空旷死白；上限 95%，防止文字溢出）；
>     - **并排卡片相邻极差自然交替 (Natural Alternating Variance)**：拒绝机械式将所有框强行塞满相同比例，并排卡片相邻极差不强制处处相同，呈现“时小时大”的人工自然节奏：
>       - 大极差比例约为 50%（相邻极差 $\ge 25\%$，如 92% 高密卡与 63% 透气卡相邻，形成鲜明呼吸）；
>       - 小极差比例约为 50%（相邻极差 $5\% \sim 25\%$，如 85% 与 75%，平缓过渡）；
>     - **默认行距与弹性调节机制 (Line Spacing Flexibility: Default 18~20pt, Allowed 17pt or 21~23pt)**：
>       - 默认行间距为 **18 ~ 20 pt**（或 1.25 ~ 1.35 倍）；
>       - 行间距不强制锁死 18~20pt：若卡片确实偏小（文字密集）或偏大（框深），且文字调整到 60%~95% 填充率后仍无法完美平衡，允许微调至 **17 pt**（紧凑小框）或 **21, 22, 23 pt**（舒展深框）；
>     - **同页行间距一致性铁律 (Same-Page Line Spacing Consistency)**：**同一页内所有文本框的行间距最好/必须保持一致**（`同一页的行间距最好一致，不同页的无所谓`），严禁在同一页内混用 16pt 与 18pt 或 21pt 导致肉眼可见的排版杂乱。
> 37. **混排多栏高度与非对称水印协调律 (Hybrid Layout & Watermark Coordination)**：
>     - 左下角含湖南大学水印印章时，左侧卡片避让缩小（$h \approx 94\text{ pt}$）属于正常非对称排版；但右侧并排高卡片（$h=240\text{ pt}$）**内容量必须与深度高度对齐**，必须配置 3 条充分展开的饱满分点，绝不能留下整整半边死白；右侧卡片底色必须融入全页 2~3 色调和配色，严禁突兀异色。


---

## 一、排版编译核心逻辑：四步闭环设计法 (The 4-Step Academic Pipeline)

这是本技能不可违背的**底层核心逻辑与执行铁律**。制作任何学术演示文稿，必须严格遵循以下四步闭环推进：

```
+---------------------------------------------------------------------------------------------------+
| 步骤 1: 深度读取素材       --> 提取真实核心技术脉络与白纸黑字量化指标，100% 拒绝虚构与概念捏造     |
| 步骤 2: 容量规划与划分总页数 --> 确立整体论证主线与总页数，推行“单页单主题 (One Slide One Topic)” |
| 步骤 3: 多模态图文精准分配 --> 真实提取素材高清图片/响应曲线/架构拓扑与学术三线表，图文互证      |
| 步骤 4: 模板匹配与全要素微调 --> 匹配 L01~L12 原型，标题<=17字/正文16~20pt/图像适度微调拉伸贴合边框 |
+---------------------------------------------------------------------------------------------------+
```

1. **步骤 1：深度读取素材 (Deep Material Ingestion)**
   - 彻底吃透技术文件（如招标文件、科研论文、申报书）的核心逻辑；
   - 绝不机械复制粘贴全文，提炼出真实机理、核心公式、参数边界、量化指标与闭环结论；
   - **绝对杜绝虚假概念**：严禁出现参考材料中未提及的任何中间件、未验证的频率与时延数值。
2. **步骤 2：容量规划与总页数划分 (Capacity Planning & Logical Slicing)**
   - 根据素材总量与汇报目标，精确确立总页数（深入版 15~18 页）；
   - **单页单主题 (One Slide, One Logical Theme)**：每一页只讲透一个核心主题，页面之间层层递进、逻辑闭环（总体架构 -> 飞控动力学 -> 硬件平台 -> SLAM感知 -> B样条轨迹优化 -> EGO-Swarm集群 -> 仿真基准 -> <YOUR_TOPIC> -> 地面大屏 -> 应急处置 -> 硬件看门狗 -> 软件工程化 -> 里程碑排期 -> 交付清单 -> 售后保障）；
   - 确立每页的卡片与展区容量预算，严禁前松后紧或局部大块空白。
3. **步骤 3：内容驱动的多模态精准分配 (Content-Driven Dynamic Visual Allocation)**
   - **彻底打破机械主义“每页固定两幅图”**：图量完全由本页技术主题的信息密度与机理复杂度动态决定（0~3张图自由适配）：
     - 纯应急决策/制度承诺页（0 图）：依靠层次分明的文字字阶与卡片自上而下饱满铺排，垂直充满卡片，展现工业级严谨度；
     - 宽幅高精视景/单一流程大图页（1 张大图）：如 4 阶均匀 B 样条凸包绕飞、系统总体架构、V 型开发流程，单张高清大图独占半幅；
     - 理论仿真对比页（2 张对比图）：如阶跃响应对比 + 扰动恢复放大图、点云地图 + ikd-Tree 结构，双图横向/纵向互证；
     - 高难多工况集群演化页（3 张组图）：如 EGO-Swarm 集群协同导航、窄通道队形压缩恢复、三维对冲避障 3 张典型仿真图并列；
   - **真实素材与独立子目录强绑定**：所有图片必须从参考素材中提取真实工程原图，且必须存放在 `assets/<项目素材标题>/` 专属独立子目录中，并在子目录 `README.md` 中显式注明项目标题、源素材与适用模型；严格遵循最小原则（$N_{\text{assets}} = N_{\text{deck\_needed}}$），杜绝跨项目混淆复用，严禁使用虚假占位图，缺失立即抛异常阻断。
4. **步骤 4：模板匹配与全要素自适应微调机制 (Adaptive Tuning)**
   - 选择最适配该主题的排版原型（L01~L12），并执行全维度视觉微调：
     - **封面标题与大标题对齐**：
       - **封面主标题**：严格 **$\le 17$ 个汉字**，字号 **`40.0 ~ 42.0 pt`** 居中单行，绝不折行，右侧留足 $\ge 40\text{ pt}$ 安全留白；
       - **页面大标题**：**`26.0 ~ 28.0 pt`** 微软雅黑加粗单行，**最大不超过 28.0 pt**，左边缘严格对齐 `X = 38.1 pt`；
     - **字号阶梯自适应与双模态分流（铁律）**：
       - **小字号专为大容量服务**：若内容适中（2~3个要点），**正常正文文字严格按 `16.0 ~ 18.0 pt` 设定**（极简可用 19~20pt）；
       - **高密度技术详述模式**：若内容多、技术机理丰富、分点多（4~7个要点），正文字号**以 `14.0 ~ 15.0 pt` 为主**（如 14.5pt / 15.0pt），同一页内极差严格 $\le 1.0\text{ pt}$；
       - **卡片子标题**：**`19.0 ~ 22.0 pt`** 微软雅黑加粗（湖大红 `#A6232B`）；
       - **密集表格单元格、图注、次级说明**：**`14.0 ~ 15.0 pt`**；
       - **全域绝对底线**：**最小就是 14.0 pt，绝对不允许任何文字低于 14.0 pt**！
     - **三层间距解耦与大范围灵活缩放**：
       - 框间距允许在 **`0 ~ 16 pt`** 大范围动态调节，图大字多时收缩至 `6 ~ 10 pt` 或 `0 pt`；
       - 容器内部 Padding 支持设为 0（贴边扩展）或 8pt（标准呼吸），消灭“A-A框-B框-B”多重间距叠加的空旷感；
     - **垂直充满与行间距微调**：文本框采用顶部对齐，默认行间距 **`1.25x`**，段后距基于目标高度（0.88）动态反算均分（`4.0 ~ 12.0 pt`），卡片垂直填充率稳定在 **$85\% \sim 95\%$**，消除底部死白；
      - **原生数学公式体与角标下沉微调 (Native Math & Subscript Engine)**：
        - 涉及复杂目标函数与物理公式时，严禁使用普通文本模拟，必须通过底层注入原生 DrawingML 14 Office Math 结构树（`<a14:m><m:oMathPara><m:oMath>...</m:oMath></m:oMathPara></a14:m>`）；
        - **颜色显式注入铁律**：公式内每个 Run 的 `<a:rPr>` 必须显式赋予深灰文字色（`<a:solidFill><a:srgbClr val="242424"/>`），彻底根除因继承母版默认白色而在白底卡片上“隐形”的重大缺陷；
        - 正文段落中的物理变量角标（如 $J_s, \lambda_c$）必须通过 `<a:rPr baseline="-25000">` 实现物理角标下沉，彻底消灭下划线 `_`；
      - **彻底消除 0.75pt 细线碎框与宏观大卡片包裹 (Macro Card Clustering)**：
        - 废除所有单个文本框、图片框上的 `0.75pt #E2E8F0` 细线描边，默认设置为纯净无边框卡片（`line.fill.background()`），实现纯净悬浮质感；
        - 相似主题、并列关系或推导关联的一组图文组件，采用宏观圆角矩形浅色大框包裹（边框略粗、圆角自然），组件在内部自适应对齐铺排，杜绝碎框；
      - **表格 0 内边距、全列水平居中与垂直居中对齐 (Zero Margin & Double Center Table)**：
        - 表格所有单元格上下左右边距强制置零（`margin_left = margin_right = margin_top = margin_bottom = Pt(0)`）；
        - **所有列文字强制水平居中（包括第一列）**：`p.alignment = PP_ALIGN.CENTER`，彻底根除第一列靠左导致的不规整视觉缺陷；
        - 单元格文字强制垂直居中（`vertical_anchor = MSO_ANCHOR.MIDDLE`），杜绝文字下坠或悬空；
        - 彻底消除沉闷深黑底板，表头采用**淡雅浅陶粉 (`#F8EAEB`)** 或 **温润浅米灰 (`#F4EFEA`)**；
        - 表头文字采用**湖大红深色加粗文字 (`#8B1D23`, 15pt)** 或 **深炭灰 (`#2D3748`, 15pt)**；
        - 交替行斑马纹 (`#FDFBFA` / `#FFFFFF`)，浅灰细边框 (`#E2D9D7`)，无粗重竖线；
      - **图像长宽比深度适配与断句艺术 (Aspect-Ratio Alignment & Typography)**：
        - 严禁“大框装小图”或“3:2 框塞 2.4:1 宽扁图”：必须根据图像真实长宽比 $AR = W / H$ 动态开辟容器宽度；
        - 宽扁横图（如 $AR \ge 2.0$ 的链路图）必须将容器展宽至 $\ge 350\text{ pt}$，压缩侧边文字框；
        - 狭窄文字框（宽约 235pt）内的标题必须人工精细断句折行（如“空地异构设备\n联合自主导航与搜索”），正文采用 14pt 适度拉大行距垂直撑满，消除文字下方死白；
      - **形状防重叠安全边界检算**：精确计算表格撑大后的实际包围盒，确保与下方卡片保持至少 25 pt 安全间隙，杜绝物理重叠。


---

## 二、核心触发与专属规范 (HNU Dedicated Mainline)

> **触发条件**：
> 凡当用户在需求中提及 **“湖南大学PPT模板”**、**“湖大PPT模板”**、**“湖大模板”**、**“HNU PPT”** 或指定湖大组会风格时，**启用本技能**！

### 1. 核心底板与母版继承规范
- **底板基准与共享素材**：
  `templates/hnu_base_template.pptx`（源自 `D:\win-linux-share\AIM\PPT-skill\可变形飞行机器人_组会汇报_参考模板.pptx`，并在 `_ppt-shared-assets/` 保持同步）。
- **标准主色调（湖大红）**：
  **`#A6232B`**（**绝对记忆色号**，用于大标题、顶部 5pt 标志红线、核心状态标签与重点高亮）。
- **第 1 页（封面页红色底框保护与单行大字号居中铁律）**：
  - 完整无损复用封面背景底图（`image1.png`，`960 × 540 pt`）；
  - 右上角湖南大学校徽（`image2.png`，`[758.05, 8.75, 160.2, 50.95] pt`）；
  - **红色半透明背景底框（形状 1 / 矩形 5）**：**绝对禁止修改其坐标与尺寸，保持 [143.55, 361.75, 816.45, 178.25] pt 绝不变小**！清空其内部文字，纯粹作为背景底色块，绝不作为文本框使用；
  - **白线分隔条（形状 6）**：严格位于 `top = 449.60 pt`，将红带完美划分为上下两半；
  - **主标题文本框（形状 5 / id=12）**：`[215.75, 361.75, 715.0, 87.85] pt`，微软雅黑加粗，**字号 40 ~ 42 pt**，纯白 `#FFFFFF`，**垂直居中对齐 (`vertical_anchor=MIDDLE`)**，**强制单行 (`word_wrap=False`)，绝不折行**，**标题字数严格 ≤ 17 个汉字，绝不允许超出 PPT 最右侧**（右侧留白安全余量 $\ge 40\text{ pt}$）！左对齐起始位置与下方的“汇报人”分毫不差垂直共线；**全页唯一主标题，严禁在其它形状中重复写入标题造成文字重叠**；

  - **汇报人与时间文本框（形状 4 / id=2）**：`[215.75, 449.55, 650.0, 90.40] pt`，微软雅黑加粗，**字号严格为 24 pt**，纯白 `#FFFFFF`，**垂直居中对齐 (`vertical_anchor=MIDDLE`)**，单行无折行；
  - **多余文本框彻底清空**：形状 2（文本框 6）与形状 3（文本框 7）必须彻底清空文本，严禁写入任何文字。
- **内容页标准母版骨架 (Header & Footer) 与单行对齐铁律**：
  - **单行大标题**：`[38.1, 12.0, 700.0, 48.0] pt`，字号 **26 ~ 28 pt** 微软雅黑加粗，颜色 `#A6232B`，最大不超过 28pt。**左侧起始位置严格与模板“共”字及下方内容卡片左边缘垂直平齐共线 (`X = 38.1 pt`)**，右侧延伸至 700 pt（终止于右上校徽前），**强制单行 (`word_wrap=False`)，绝不允许两行折行**；
  - **顶部标志性红线**：`[-0.75, 71.5, 961.55, 0.0] pt`，横贯全幅，线宽 `5.0 pt`，颜色 `#A6232B`；
  - **右上角校徽**：`[758.05, 8.75, 160.2, 50.95] pt`；
  - **左下角校徽水印印章**：`[11.55, 356.05, 271.75, 183.95] pt`（**每一页内容页均强制保留，不可删除，不可被内容遮挡！**）；
  - **右下角页码**：`[894.0, 513.75, 36.0, 19.5] pt`，**14.0 pt 微软雅黑**，颜色 `#666666`，格式化为两位数（`02`, `03`...）；
  - **结束页规范**：基准母版中的 Slide 2（“汇报完毕，请批评指正！”）为官方标准结束页。生成新文稿时，引擎自动将该结束页移至全套幻灯片的**最后一页**，形成完美汇报闭环；
  - **非模板遗留内容彻底清理铁律**：原模板参考页中的特定问题标题（如“共同问题：如何让机身形变服务通行与抓取？”）和旧表格（如“研究线/核心问题/主要贡献”）**纯属旧课题参考样例，绝非模板固定构件！在克隆母版构建新内容页时必须 100% 彻底清除，严禁残留任何旧文本或旧表格**。

### 2. 非对称直角多边形可绘制区与全域充实排版铁律
为了避让左下角校徽印章并充分利用屏幕空间，内容区域呈现直角“L”型多边形包络：
- **通行区域**：
  - 顶部全宽区：`X: 38.1 ~ 918.25 pt (宽 880.15 pt), Y: 95.0 ~ 345.0 pt (高 250.0 pt)`
  - 右下延伸区：`X: 296.0 ~ 918.25 pt (宽 622.25 pt), Y: 355.0 ~ 495.0 pt (高 140.0 pt)`
  - 左下禁区：`X < 280.1 pt 且 Y > 350.0 pt`（**绝对禁飞区**，严格保留给印章，绝不可放置任何内容块或卡片！）
- **排版充实法则（消灭任何孤立死角空白）**：
  1. **右下完整落地承接律**：在印章上方卡片截断后，右下延伸区必须横向由宽幅落地卡片或宽图完整横贯（`X: 296.0 ~ 918.25 pt`，宽 622.25 pt），**严禁在中间下部留出大面积近方形死角空白**；
  2. **双字体锁定与 East Asian 标签硬注入**：
     - 普通中文一律使用**微软雅黑（Microsoft YaHei）**；
     - 英文、数字、变量与公式均支持微软雅黑与 Cambria Math；
     - 在底层 OpenXML DrawingML 中，所有文字必须同时显式写入 `<a:ea typeface="Microsoft YaHei"/>` 与 `<a:latin typeface="Microsoft YaHei"/>`，彻底杜绝 Windows PowerPoint 回退等线体或宋体；
  3. **底层架构零损坏保证**：核心生成引擎统一基于 `HNUTemplateEngineHNU` (`python-pptx`) 构建，确保生成后的 PPTX 在 Microsoft PowerPoint 中**无任何“内容发现问题需要修复”的弹窗警告**，100% 原始打开。

### 3. 学术高级排版与语义质量铁律 (Rules 34~38)

- **Rule 34: 语义色彩调和律、2~4 色配比与湖大红纯白高对比度铁律 (QG-34)**：
  - 彻底废除按列模运算循环（`[BLUE, GREEN, ROSE, WARM]`）的“马卡龙彩虹圈”。
  - 单页主题色严格 $\le 4$ 种（不含纯白背景与深灰文字），推荐 2~3 种语义协调色。
  - **严格语义映射**：
    - **底色湖大红 (`#A6232B` / `#F8EAEB`)**：各级标题、流程粗箭头、章节高亮、核心痛点对比；
    - **学术深蓝/板岩灰 (`#1D4999` / `#EAF1F6`)**：技术底座、通用流水线步骤、基础组件平台；
    - **鼠尾草灰绿 (`#0F766E` / `#EEF3E7`)**：工程规则、通过验证的最终成效、沉淀复用的 Skill 资产；
    - **暖赭石/琥珀黄 (`#B45309` / `#FEF3C7`)**：**严格专用于负面/警示语义**（算力成本超标、踩坑记录、版本漂移风险），严禁滥用于中性模型或选型卡片。
  - **湖大红深色背景文字高对比度纯白铁律 (High Contrast Pure White Text on HNU Red)**：
    - 凡以**湖大红 (`#A6232B` / `#8B1D23` / 深红)**或深色作为底托/填充背景的文本框、卡片或强调色块，**其内部承载的标题、正文、分点及标注文字必须统一强制使用纯白色 (`#FFFFFF` / `RGBColor(255, 255, 255)` / `schemeClr val="bg1"`)**；
    - **严禁在湖大红深色背景上继续沿用深炭灰 (`#242424`)、黑色或暗红文字**（如早期第 18 页出现的黑字配红底），杜绝文字隐形，彻底保障学术报告在投影或屏幕阅读下的高对比度与清晰可读性。

- **Rule 35: 核心分点自适应展开与客观事实知识扩充律 (QG-35)**：
  - **弹性分点机制 (2 ~ 5 条)**：彻底打破“固定 2 条分点”的机械教条！分点数量完全由卡片可用尺寸与信息深度动态决定：常规精炼卡片 2~3 条，高密技术卡片 3~5 条；
  - **长难句主动拆解**：严禁在单个分点内堆砌长难句；长句必须拆解为多个独立分点，每点聚焦一个核心机制或参数维度；
  - **客观事实扩充赋权**：在描述通用技术、开源大模型架构与科研流水线等**客观事实**时，允许并倡导结合领域常识进行充实的客观技术事实分点扩充。

- **Rule 36: 有机饱满度、默认 18~20pt 行距防虚假降级与同页一致性铁律 (QG-36)**：
  - **垂直饱满度判定区间：严格 [60% ~ 95%]**（底线 60%，消除空洞死白；上限 95%，防止文字溢出；物理溢出阈值 108%）；
  - **并排卡片相邻极差自然交替 (Natural Alternating Variance)**：坚决杜绝“全篇卡片字数完全相同、齐刷刷停在同一列左侧”的机械僵硬感！并排卡片相邻极差不强制处处相同，呈现“时小时大”的人工自然节奏：
    - 大极差比例约为 50%（相邻极差 $\ge 25\%$，如 92% 高密卡与 63% 透气卡相邻，形成鲜明呼吸）；
    - 小极差比例约为 50%（相邻极差 $5\% \sim 25\%$，如 85% 与 75%，平缓过渡）；
  - **默认 18~20pt 行距权威基准与防虚假降级铁律 (Default 18~20pt & Anti-False-Degradation)**：
    - 默认正文行间距必须严格为 **18 ~ 20 pt**（基准推荐 **18.5 pt**，或 1.25 ~ 1.35 倍）；
    - **严禁虚假降级至 17.0 pt**：17.0 pt 仅作为极端狭小卡片的最后应急兜底选项！**凡底托卡片可用高度足以容纳 18.5 pt 标准行距者（$N_{\text{lines}} \times 18.5\text{ pt} \le H_{\text{card\_avail}}$），绝对禁止降级到 17.0 pt**；
    - **行距密度判定基准**：行距密度必须基于**底托卡片可用高度**（$H_{\text{card}} - 41.0\text{ pt}$）计算，绝对严禁基于收缩包裹（shrink-wrapped）文本框自身的临时高度计算，彻底根治如第 34 页卡片下半截仍有充裕留白却被虚假降级至 17.0 pt 的系统性缺陷；
    - **开阔卡片舒展行距**：卡片框深且开阔时，允许弹性调大至 **21, 22, 23 pt**；
  - **同页行间距完全一致铁律 (Same-Page Line Spacing Consistency, 方差 = 0.0 pt)**：**同一页内所有正文文本框的行间距必须保持完全一致**（`同一页的行间距必须完全一致，不同页可根据容量独立规划`），严禁在同一页内混用 17pt 与 18.5pt 或 21pt 导致肉眼可见的排版杂乱。

- **Rule 37: 混排多栏高度与底托卡片物理边界不可逾越铁律 (QG-37 & QG-30)**：
  - 同一行并排卡片高度必须严格物理齐平（$\Delta h = 0$）。
  - **底托卡片物理边界不可逾越铁律 (Physical Card Boundary Confinement Invariant)**：
    每一个正文文本框、标题框必须严格物理收敛于其底托背景卡片（`shape.name == "BG"` 或底层实色矩形）的几何边界之内：`textbox.top + textbox.height <= card.top + card.height + 1.0 pt`。严禁任何文本框穿透底托下边沿溢出甚至压盖下方相邻卡片！
  - **140pt 流程/里程碑卡片容量刚性预算**：
    针对高 140 pt（或 $h \le 150\text{ pt}$）的横向流程/里程碑卡片，扣除顶部标题与留白后，可用正文高度仅为 95 pt。在 18.5pt 行距下，**总行数硬上限为 4 行**（推荐 2 条分点各 2 行，每条汉字数 $\le 24$ 字，卡片总字数 $\le 48$ 汉字）。严禁塞入 6 行文字导致必然穿模！
  - 官方水印（如逸夫楼手绘 `图片 9`）自然坐落在左下角开阔处，右侧卡片统一高度（如 $h=240\text{ pt}$），消除压在水印头顶的丑陋小残块。

- **Rule 38: 事实广度对齐律与反过度概括铁律 (QG-38)**：
  - **标题与内容深度对齐**：凡标题涉及长周期演进、多技术路线或多厂商对比时，页面卡片必须全面覆盖核心主流产品谱系与代表性技术节点；
  - **严禁过度概括导致失真**：当主题跨度大时，必须按技术逻辑合理分立展开（如将 2024~2026 年模型演进合理拆解为自主开源与前沿闭源多模态等多维谱系），完整呈现 DeepSeek (V2~V4.1)、GLM (4~5.3)、OpenAI GPT (4~GPT-6 Astra)、Google Gemini (1.5~3.8 Flash)、小米 MiMo (v2~v2.6 Pro) 等主流分支，严禁单薄走过场！

- **Rule 39: renhua 人性化语言风格、学术零人称、大厂黑话全面清洗与要点加粗铁律 (QG-39)**：
  - **学术叙述零人称规范 (Zero Pronoun Invariant)**：
    - 学术汇报与技术文稿中**严禁出现第一/第二人称主语（如“我”、“你”、“他”、“我们”、“大家”）**（如严禁写“我希望减少反复拖动文本框的时间”）；
    - 一律改用客观、平实、目的导向的工程白描表达（如“**为了减少反复拖动文本框的时间**：……”、“**针对文献漏检痛点**：……”、“**为打通三端联动流程**：……”）；
  - **分点开头核心论点/动机强制加粗 (Bullet-Lead Bold Rule)**：
    - **听众抓手原则**：分点开头必须将根本原因、痛点关键词或核心技术动作显式加粗（如 `**根本动机**：...`、`**手工排版瓶颈**：...`、`**断点回溯机制**：...`）；杜绝没有视觉重心的平淡平铺叙述，让观众一眼抓住核心信息！
  - **大厂黑话与伪架构虚词全面清洗 (Purge Big-Tech & Pseudo-Architecture Buzzwords)**：
    - 坚决杜绝“每个词都认识、连起来不知道在说什么”的互联网/大厂黑话与夸大叙述：
      - “壁垒” $\to$ 改为“难以直接通信的困难 / 阻碍”；
      - “资产 / 可复用资产” $\to$ 改为“具体的工程规则 / 可复用的操作规范 / 积累下来的方法”；
      - “原子化” $\to$ 改为“单项基础操作 / 单个具体功能 / 细分步骤”；
      - “沉淀 / 沉淀为” $\to$ 改为“整理成规范 / 写成规则 / 落实为具体工程规则”；
      - “高频科研方法” $\to$ 改为“常用的科研方法”；
      - “标准化通信与状态感知” $\to$ 改为“把外部工具连接进来，让程序随时调用并了解运行状态”；
      - “自愈闭环 / 闭环” $\to$ 改为“排查并自动修正问题 / 形成完整执行流程 / 形成完整的处理链条”；
      - “解耦” $\to$ 改为“拆开独立处理 / 分开配置互不干扰”；
      - “异构工具” $\to$ 改为“各类不同的外部工具”；
  - **强叙述感与直白工程解释 (Narrative Flow - "叙述感与人话")**：
    - 严禁冷冰冰的口号式定义或机械概念罗列。每一个工具、Skill 或机制，必须讲清楚：① 这个工具具体是干嘛的？② 解决了什么让人头疼的实际麻烦/痛点？③ 最终达到了什么具体效果？
    - *典型工科白描对照*：
      - ❌ *不说人话*：“MCP 协议解决异构工具的标准化通信与状态感知，Skill 则将研究者的思维逻辑与验收规则固化为可复用资产”；
      - ✔️ *工科白描人话*：“MCP 负责把各类外部工具连接进来，让程序能随时调用并掌握当前的运行状态；Skill 则把常用的科研方法、排版规范与严格的检查要求写成具体的工程规则，从而约束整体的排版与执行”；
  - **全篇每一个文本框逐一斟酌**：
    - PPT 中每一个文本框的内容都必须符合常用的科研与工程语境，杜绝生造词，让听众“一读就懂”；
  - **彻底去 AI 腔、说人话 (De-AI Tone & Plain Speech)**：
    - 坚决杜绝空洞的 AI 腔、套话、官话、客服式话术或机械喊口号；
    - 严禁使用浮夸虚饰用语：如“在飞速发展的时代”、“具有划时代的里程碑意义”、“不仅……而且……”、“显著提升/显著优势”、“极大地赋能了”、“旨在打造……”等；
    - 遇到“赋能”改“帮助/支持”；遇到“抓手”改“工具/方法”；遇到“链路”改“流程”；遇到“底层逻辑”改“原理”；遇到“痛点”改“实际问题”；
  - **客观事实与术语守恒 (Strict Factual Grounding)**：
    - 严禁为了“去 AI 味”而删减真实技术细节或篡改事实；
    - 领域专业术语（如 DeepSeek MLA、MoE、MTP、R1 CoT、GLM-4 AllTools、MiMo 2.6 Pro、MCP、Skill 等）必须精准保留；
  - **标点克制与排版洁净 (Clean Punctuation & Layout)**：
    - 严禁破折号（——），严禁分号（；），冒号克制，不用人工字符拼凑项目符号（如 `"• "` 或 `"- "`），统一依托原生 XML 悬挂缩进；中英文与数字之间保留标准空格。

- **Rule 40: Skill 叙事逻辑三部曲、痛点先行与大纲聚合铁律 (QG-40)**：
  - **痛点与动机先行原则 (Pain Point & Motivation First)**：
    - 学术逻辑因果必须严密，**严禁一上来就悬空讲解 Skill 的代码或构建过程**！
    - 每个要讲述到的 Skill，必须拥有专门的一段、一页或独立的显式区域，先讲透**“为什么做”、“解决什么痛点”、“原始问题是什么”**，再讲“如何构建与运作”，最后讲“沉淀效果”；
    - **全系核心 Skill 构建根源与真实动机对齐表**：
      - `academic-native-ppt-design-HNU-style`：**针对科研汇报与组会展示需求**，因现有湖大模板格式不统一、线条生硬不够美观，且每次手工排版极度耗时、成果质量不稳定。以官方模板为好模具，提炼出固定美观逻辑与零损坏工程规范；
      - `draw-style`：**针对 AI 绘图“一眼假”、塑料感重、不学术且提示词反复调试极度低效的痛点**。沉淀顶会论文图纸级配色体系与几何拓扑规约，实现高质量、固定形式的一键出图；
      - `renhua`：**针对现有 GitHub 高星降 AI 味工具过于生硬、不符合中文自然科研表达习惯的痛点**。自建人性化白描规范，消除机械套话与公文黑话，说人话、求真务实；
      - `drone-literature-scout`：**针对选题初期不知道研究方向、海量文献不知从何下手的迷茫痛点**。借助 AI 定时自动化爬取、过滤与调研前沿方向，作为首个自建探索型 Skill；
      - `zotero-obsidian-paper-import`：**针对人肉逐篇搜索容易漏检、文献与笔记割裂的痛点**。打通 Codex-Zotero-Obsidian 三端联动，在来源准确前提下自动入库 PDF，并在笔记知识点中直链跳转原文；
      - `read-paper-analysis-highlight`：**针对长篇精读效率低痛点**，快速提炼核心创新与数学机理，并以个人习惯对论文 PDF 施加原生高亮与批注；
      - `obsidian-note-style`：**针对手敲手写笔记速度慢痛点**，让 AI 学习已有笔记素材沉淀的结构与语言习惯，自动生成体系化知识卡片；
      - 治理类 Skills (`skill-contract-lock`, `governor`, `collect-bug`, `workspace-hygiene`)：**针对多 Skill 协作下规则冲突、依赖漂移、Bug 重复踩坑与环境污染痛点**，构建自愈闭环与环境整洁规范；
  - **相同 Skill 页面绝对连续律 (Contiguous Skill Pages)**：
    - 同一 Skill 的多张页面必须在结构上**紧密相连、一气呵成**，严禁跨 Skill 穿插割裂（如严禁在 `read-paper-analysis` 页面中间穿插 `obsidian-note-style` 页面，必须将 read-paper 全部连续讲完后，再进入 obsidian-note-style 体系！）；
  - **需求驱动核心哲学 (Core Grounding Philosophy)**：
    - 必须在文稿显要位置向听众明确：**无论哪一个 Skill，其核心都是根据研究者的真实需求建立的；只有需求描述得足够清楚，AI 才能发挥最大功效；AI 是强力辅助，但绝无法替代研究者的学术判断与主导地位**；
  - **篇幅弹性与动态伸缩律 (Dynamic Page Elasticity)**：
    - 演示文稿总页数应根据内容逻辑饱满度动态自适应（基准在 75 ~ 85 页区间内弹性浮动），严禁为了死凑某个固定数字而导致页面排版过挤、过窄或大面积留白。

- **Rule 41: 零 Markdown 字符泄漏与原生 OpenXML 富文本渲染契约 (Zero-Markdown-Leak Invariant, QG-41)**：
  - **严禁裸露 Markdown 标记字符 (Zero Raw Markdown Symbols)**：
    - 在最终交付的 PPT 演示文稿中，**正文、标题、图注、表格单元格与卡片列表内绝对严禁出现任何裸露的 Markdown 控制字符**（包括但不限于 `**加粗**`、`__下划线__`、`## 标题`、`` `行内代码` ``、以及手动列表符号 `* `、`- ` 等）；
    - **学术汇报大忌**：直接在 PPT 文字中显示 `”**外部工具连接**“` 是学术汇报的绝对禁忌（大忌），彻底暴露了未经渲染的 AI 原生直接复制痕迹，严重损害学术严谨性；
  - **原生 DrawingML Run 渲染契约 (Native DrawingML Run Property)**：
    - 所有的文本加粗、倾斜、下划线效果，**必须且只能通过 PPTX 原生 OpenXML Run 属性**（如 `<a:rPr b="1">` 或 `run.font.bold = True`）显式赋予；
    - 在将文本写入 Run 之前，代码引擎必须通过严格的正则清洗逻辑（如 `re.sub(r'\*\*(.*?)\*\*', r'\1', text)`）彻底剔除所有 Markdown 语法包裹符号；
  - **分点开头要点原生加粗范式 (Bullet-Lead Native Bold Pattern)**：
    - 对于分点开头的重点提炼词（如“**外部工具连接**：”），必须拆解为独立的 Run 分别赋值：
      - 前缀 Run：`run_lead.text = "外部工具连接"`, `run_lead.font.bold = True`, `run_lead.font.color = base.b.rgb(base.b.INK)`；
      - 后续 Run：`run_body.text = "：MCP 连接各类外部工具..."`, `run_body.font.bold = False`；
    - 严禁为了偷懒而将带 `**` 的原始 Markdown 字符串一次性赋予单个 Run 的 `.text` 属性！

---

## 二、从 HTML-PPT v3 中汲取的精髓与技术重构

在 v2 版本中，系统全面沉淀了 `academic-native-htmlppt-design-v3-acad` 的学术级设计法则，同时完成了从 Web 到 PPTX 的技术范式迁移：

### 1. 汲取并继承的核心法则
- **学术提炼本质**：“PPT 的内容是绘制重点，并不代表要绘制所有的内容”。严禁全文机械堆砌，提炼技术机理、核心公式、量化指标与闭环结论。
- **全域字阶梯队（最小 14pt，正文 16~20pt，大标题 ≤ 28pt 铁律）**：
  - 页面大标题：`26 ~ 28 pt`（微软雅黑加粗，`#A6232B`，**最大不超过 28 pt**）
  - 核心一句话结论 / 卡片子标题：`20 ~ 22 pt`（微软雅黑加粗）
  - **正常正文文字**：**`16 ~ 20 pt`**（根据当页排版与文本量自适应微调：密集 16pt，常规 17~18pt，精炼 19~20pt）
  - 密集技术注释、公式解释、表格单元格、图注：`14 ~ 15 pt`（**全域绝对底线 $\ge 14\text{pt}$，严禁低于 14pt**）
  - 底部页码：`14 pt`（微软雅黑常规，双位数）
  - 封面大字号：主标题 `40 ~ 44 pt`，汇报人 `24 pt`
- **双字体绝对纯度**：
  - 普通中文一律使用**微软雅黑（Microsoft YaHei）**，杜绝宋体混入；
  - 英文、数字、物理变量与数学公式一律使用 **Cambria Math**（变量斜体，算子正体）。
- **几何防穿模与自适应行距**：文本框显式设置 `6 pt` 内边距，计算行长与文本容量，动态调整段后距（`3 ~ 6 pt`），坚决杜绝文字穿透边框或出现单字孤行。
- **非对称充实构型与图像自适应拉伸**：打破僵硬的五五开对半分，利用自然直角分割，消除大面积无用留白；图像允许在 $\pm 5\% \sim 10\%$ 内做几何微调和轻度拉伸，紧密咬合卡片边界。

### 2. 彻底剔除的 HTML/CSS 专属规则
- ❌ **剔除 CSS 盒模型与弹性/网格布局**：彻底抛弃 `display: flex`、`grid-template-columns`、`gap`，转换为 PPTX 原生 DrawingML 的绝对坐标 `(x, y, w, h)` 体系。
- ❌ **剔除 Web 滤镜与阴影**：抛弃 `backdrop-filter: blur()`（毛玻璃）、CSS 渐变与多层投影，转为原生实色卡片与矢量细边框。
- ❌ **剔除 DOM 语义标签**：抛弃 `<div>`, `<span>`, `<table>`, `<svg>`，转为 PPTX 原生 TextFrame、Table、Shape、Picture 对象。
- ❌ **剔除外部 CDN 与 WebFont**：彻底清除 KaTeX CDN 网络依赖，完全基于系统内置的 Cambria Math 矢量字形实现 100% 离线高清渲染。
- ❌ **剔除 Headless 浏览器截屏**：严禁以网页截图方式交付，保证每一个文字、形状均可在 PowerPoint 中直接选中与修改。

详尽字阶规范请查阅：[字体排版与数学公式规范](references/typography-math-spec.md)。

---

## 三、12 套基础排版模型速查 (L01 ~ L12)

根据汇报内容与素材特征，智能匹配最佳排版模式：

| 模式 | 对应模板 | 块数 | 空间结构特征 | 最佳适配学术场景 |
| :---: | :---: | :---: | :--- | :--- |
| **L01** | Slide 2 | 2 | 顶通栏横幅 (848×184) + 底右卡 (602×137) | 全景对比表/跨系统长流程 + 底部结论承接 |
| **L02** | Slide 3 | 2 | 顶主展区 (848×226) + 底右落地卡 (602×137) | 宽幅机理拓扑/三联实验对比 + 指标落地 |
| **L03** | Slide 4 | 2 | 左正方卡 (229×229) + 右全景大图 (607×376) | 文字精炼（3-5条参数）+ 高清整机/飞行大图 |
| **L04** | Slide 5 | 3 | 左方卡 (229×229) + 右上双展 (606×229) + 右下沉淀 (606×134) | 设计动机 + 变形前后双图 + 动力学方程 |
| **L05** | Slide 6 | 3 | 左方卡 (229×229) + 右上横幅 (605×87) + 右下深层区 (605×276) | 核心问题 + 阶段里程碑 + 核心控制律大图 |
| **L06** | Slide 7 | 4 | 顶展区 (845×144) + 中过渡 (847×69) + 底双卡 (294×137 ×2) | 全景架构 + 时钟同步协议 + 软硬件双分支验证 |
| **L07** | Slide 8 | 4 | 顶大图 (848×156) + 中说明 (848×61) + 底双非对称卡 | 实验全流程 + 状态约束 + 左右非对称收敛对比 |
| **L08** | Slide 9 | 3 | 左方卡 (229×226) + 中竖长柱 (191×375) + 右大屏 (396×375) | 输入参数 + 纵向状态机流转 + 三维数字孪生视景 |
| **L09** | Slide 10 | 4 | 左双叠卡 + 中竖长柱 (191×375) + 右大屏 (396×375) | 分层约束判据 + 纵向协议栈 + 大图实测结果 |
| **L10** | Slide 11 | 3 | 左上宽卡 (438×227) + 中下短卡 (195×137) + 右全高屏 | 理论建模 + 关键配置 + 全轨迹实飞对比 |
| **L11** | Slide 12 | 3 | 顶层双宽卡并排 (438×227, 396×227) + 底宽长卡 (605×137) | **经典双方案/双模态并列对比** + 联合控制律与结论 |
| **L12** | Slide 13 | 3 | 三层阶梯横栏式 (848×108, 848×108, 605×137) | 感知-决策-执行三层架构或三阶段推进流水线 |

详尽图鉴请查阅：[布局原型与空间分配图鉴](references/layout-archetypes.md)。

---

## 四、自动化构建与质量审计工作流

本技能配备全自动构建引擎与质量门禁审计脚本：

```bash
# 1. 运行自动化构建引擎生成 PPTX
python C:\Users\w5711112\.agents\skills\academic-native-ppt-design-HNU-style\scripts\hnu_template_engine.py --test

# 2. 运行自动化质量门禁执行全量合规审查
python C:\Users\w5711112\.agents\skills\academic-native-ppt-design-HNU-style\scripts\audit_hnu_deck.py --input <path_to_deck.pptx>
```

### 自动化审查标准 (Quality Gates)
1. **QG-01 原生可编辑性**：无整页位图贴片，文字与形状均可在 PowerPoint 中原生选中与修改；
2. **QG-02 导引框虚拟化**：无残留蓝色参考多边形或矩形框，全面呈现纯净卡片；
3. **QG-03 字阶规范红线**：全域文字绝对底线 $\ge 13.0\text{ pt}$，正常正文文字必须在 $14.0 \sim 18.0\text{ pt}$ 之间，页面大标题 $\le 28.0\text{ pt}$，卡片子标题 $17.0 \sim 21.0\text{ pt}$；
4. **QG-04 双字体纯度**：全域中文字体 100% 绑定微软雅黑（同时写入 `<a:ea>` 与 `<a:latin>`，绝无宋体/等线回退）；
5. **QG-05 封面红框保护**：封面红色底框尺寸不变，文字垂直居中单行无重叠，主标题字数 $\le 17$ 字；
6. **QG-06 禁区避让与非对称充实**：无形状侵入左下角校徽印章区域，右下横贯落地消除方形空白；
7. **QG-07 事实真实性**：绝对忠实于原始事实（如均匀 B 样条、FAST-LIO2、ADRC），严禁捏造虚假概念；
8. **QG-08 模板色彩保真**：大标题与顶部红线严格使用 `#A6232B`（湖大红）；
9. **QG-09 真实工程图片实装率**：全篇真实工程高清图实装 $\ge 15$ 张，绝无空图框或虚假占位图；
10. **QG-10 draw-style 淡雅三线表**：表头淡雅浅陶粉/温润浅灰底板，深红/深炭灰加粗表头文字，交替浅灰斑马纹，彻底消除黑块；
11. **QG-11 形状防重叠安全边界**：表格与卡片间保持 $\ge 25\text{ pt}$ 安全间距，杜绝物理穿模重叠；
12. **QG-12 物理变量与排版纯度**：代数变量与代价函数使用 Cambria Math 斜体，严禁裸传 LaTeX 源码反斜杠；
13. **QG-13 原生数学公式体 (<a14:m>)**：公式采用 DrawingML 14 原生 OMML 结构树注入，支持 WPS/PowerPoint 官方公式设计器，显式指定深色字体防止继承母版白色导致隐形；
14. **QG-14 彻底消除 0.75pt 细线碎框**：全面废除各元素独立 0.75pt 细描边，采用无边框纯净卡片或宏观圆角矩形大框包裹；
15. **QG-15 表格 0 边距垂直居中**：所有单元格内边距设为 0，文字一律垂直中部对齐，消除悬空与折行挤压；
16. **QG-16 图框长宽比自适应与断句**：容器严格贴合图片物理长宽比（宽扁图给足 $\ge 350\text{ pt}$ 宽度），狭窄文字框（~235pt）人工精细断句并用 14pt 行距充满；
17. **QG-17 全生命周期零中间文件即时清理**：退出前彻底清理 `__pycache__`、临时测试脚本、临时图片与 XML，仅保留单一权威 `.pptx` 范例；
18. **QG-20/QG-30 底托卡片物理边界不可逾越**：文本框底部绝对不可穿透底托卡片下边缘（$\le \text{card.bottom} + 1.0\text{ pt}$），140pt 流程卡片总行数严格 $\le 4$ 行（$\le 24$ 字/点）；
19. **QG-34 湖大红底文字纯白铁律**：湖大红等深色背景卡片内所有文字强制设为纯白 (`#FFFFFF` / `bg1`)，严禁黑字红底；
20. **QG-36 默认 18~20pt 行距防虚假降级**：默认行间距必须为 18~20 pt（基准 18.5 pt），卡片空间充裕时绝对禁止虚假降级到 17.0 pt，同页方差 0.0 pt；
21. **QG-39 renhua 学术零人称白描与要点加粗**：严禁出现“我/你/他/我们/大家”，清洗大厂黑话，分点开头加粗核心论点；
22. **QG-40 Skill 叙事三部曲与痛点先行**：每个 Skill 必须先讲透“为什么做/解决什么痛点”，同类 Skill 页面绝对连续；
23. **QG-41 零 Markdown 字符泄漏铁律**：正文绝对严禁出现裸露的 `**加粗**`、`__`、`##`、`` ` `` 等符号，加粗必须依托 DrawingML 原生 run.font.bold 属性！

详尽检查清单请查阅：[质量门禁与审计规则](references/quality-gates.md)。


---

## 五、参考文档索引

- [湖南大学专属模板规范](references/hnu-template-system.md)
- [布局原型与空间分配图鉴](references/layout-archetypes.md)
- [字体排版与数学公式规范](references/typography-math-spec.md)
- [原生可编辑性契约](references/native-editability.md)
- [质量门禁与审计规则](references/quality-gates.md)
